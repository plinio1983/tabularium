# Copia questo file in deploy.conf e modifica i valori reali.
# deploy.conf e' ignorato da Git.

SERVER_HOST="178.18.248.213"
SERVER_USER="root"
SSH_KEY=".devops/contabo_rsa"
REMOTE_DIR="/app/tabularium"

# Se vuoto, lo script usa il git short SHA corrente.
# IMAGE_TAG="manual-tag"

# Repository privato Docker Hub.
IMAGE_REPOSITORY="883377/tabularium"

# Se vuoto, lo script usa IMAGE_REPOSITORY:IMAGE_TAG.
# IMAGE_NAME="883377/tabularium:${IMAGE_TAG}"

# Configurazione backup separata, trasferita sul server come backup.conf.
BACKUP_CONFIG_FILE=".devops/backup.conf"
INSTALL_BACKUP_AUTOMATION="1"
